using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Storage;
using Microsoft.Xna.Framework.GamerServices;

namespace Chimera.Game_Feature.Storage
{
    /*
    public static class StorageManager
    {
        
        private static GamerServicesComponent _gameservice;
        private static Game _game;
        public static void Initalize(Game game)
        {
            if (_gameservice != null)
                game.Components.Remove(_gameservice);

            _gameservice = new GamerServicesComponent(game);
            game.Components.Add(_gameservice);

            _game = game;
        }
        
    }
     */
}
